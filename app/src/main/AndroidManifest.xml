<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dinhvu.quanlynhatro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.MainActivity" />
        <activity android:name=".View.SignupActivity" />
        <activity android:name=".View.DetaiActivity" />
        <activity android:name=".View.KhachtroActivity" />
        <activity android:name=".View.DetaikhachtroActivity" />
        <activity android:name=".View.InforkhActivity" />
        <activity android:name=".View.HoadonActivity" />
        <activity android:name=".View.InforhoadonActivity" />
        <activity android:name=".View.SearchActivity" />
        <activity android:name=".View.DoimatkhauActivity" />
        <activity android:name=".View.Edithoadon"></activity>
    </application>

</manifest>